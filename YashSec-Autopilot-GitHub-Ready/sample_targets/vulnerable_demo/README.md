# Intentionally Vulnerable Demo

Run only on your local machine:

```powershell
npm start
```

It listens on `http://127.0.0.1:4000` and exists only to demonstrate startup detection, static findings, safe dynamic checks, OpenAPI detection, review workflow, and report export.
