client:
  name: "Demo Client"
  network_range: "192.168.10.0/24"

scan:
  mode: "sample"   # sample or live
  sample_xml: "data/sample/sample_nmap_scan.xml"
  output_dir: "scan_outputs"
  live_profile: "service_inventory"  # discovery or service_inventory

report:
  output_dir: "reports"
  html_report: "sample_client_report.html"
  json_results: "audit_results.json"
  mermaid_diagram: "network_diagram.mmd"

risk:
  critical_penalty: 25
  high_penalty: 15
  medium_penalty: 8
  low_penalty: 3
